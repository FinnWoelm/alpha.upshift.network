language: ruby
rvm:
  - 2.3.1

# Use PostgreSQL as database
services:
  - postgresql

before_script:
  - psql -c 'create database upshift_network_test;' -U postgres

# whitelist
branches:
  only:
    - master
    - development
