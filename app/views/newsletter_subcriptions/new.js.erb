$("div#create_subscription_awaiting_response").slideUp();
$("form#create_subscription .errors").html('');
$("form#create_subscription .errors").append(
  "Please check the form and try again.");
$("form#create_subscription").slideDown();
