validator = new FormValidator("#create_subscription", "pending_newsletter_subscription")
validator.clear_errors()

<%
@pending_newsletter_subscription.errors.messages.each do |field_name, error_messages|

  error_messages.each do |error_message|
%>

validator.add_error("<%= escape_javascript field_name.to_s %>", "<%= escape_javascript error_message %>")

<%
  end
end
%>
